[pytest]
; Add the backend/ directory to sys.path so tests can import the `app`
; package directly (e.g. `from app.main import app`) without installing
; it or relying on the CWD pytest is invoked from. This also helps 
; coding tools like IDEs discover and resolve tests correctly.
pythonpath = .
